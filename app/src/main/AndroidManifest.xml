<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Phase 6: real HTTP downloads (user-initiated). No other permission added. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Phase 21: download progress notifications. Runtime permission is
         NEVER requested at launch — if the user denies (or never grants) it,
         notifications are silently skipped and in-app state remains the
         source of truth. Downloads work without it. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Background downloads: the foreground service keeps the process alive
         while rows are queued/active (e.g. after the share dialog closes).
         dataSync is the accurate type; both permissions are required to
         declare and start such a service on API 28+/34+. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <application
        android:name=".NarcicTubApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/backup_rules"
        android:fullBackupContent="@xml/full_backup_content"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NarcicTub">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Share target: a small floating dialog over the sharing app
             (YouTube/Instagram → Share → NarcicTub). Receives shared TEXT
             links only — no wildcard MIME, no file intake, no broad capture.
             excludeFromRecents + noHistory keep the sharing app's task
             untouched once the dialog is dismissed. -->
        <activity
            android:name=".ui.share.ShareDownloadActivity"
            android:exported="true"
            android:theme="@style/Theme.NarcicTub.ShareDialog"
            android:excludeFromRecents="true"
            android:noHistory="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity>
        <!-- Keeps the process alive while downloads are queued/active so a
             dismissed share dialog (or app switch) never kills a download.
             Not exported — only the app itself can start it. -->
        <service
            android:name=".notify.DownloadForegroundService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />
    </application>

</manifest>
