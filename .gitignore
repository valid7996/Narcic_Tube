# Gradle
.gradle/
build/
!gradle/wrapper/gradle-wrapper.jar

# Android/IntelliJ
local.properties
.idea/
*.iml
captures/
.externalNativeBuild/
.cxx/

# SIGNING (deliberate change): the release keystore + passwords live in the
# repo so CI can produce SIGNED GitHub Releases without secrets. The repo is
# PRIVATE — anyone with write access could sign app updates, so never make
# this repository public while this exception exists.
# keystore.properties
# *.jks
*.p12
*.pfx
narcictub-release.keystore
!gradle/wrapper/gradle-wrapper.jar
